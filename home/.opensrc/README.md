# opensrc cache

`opensrc` stores fetched package and repository source snapshots here.

This dotfiles repo only tracks this note (and ignore rules); actual cache contents are fetched on demand and should not be committed.
