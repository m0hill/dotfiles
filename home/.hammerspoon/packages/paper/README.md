# Paper

Runs `~/projects/paper/paper.sh` from the Hammerspoon **Automations → Paper** menu.

Paper must already be running. The package reports the script output in a macOS notification and prevents overlapping runs.
