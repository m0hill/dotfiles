# Whisper

Hold `Option+/` to record, then release to transcribe and paste.

## Setup

1. Install `sox`: `brew install sox`
2. Open the controller menu
3. Enable `Whisper`
4. Set `Groq API Key` in the `Secrets` section

## Storage

Settings, secrets, and hotkey overrides live in `packages/whisper/whisper.json`.
