---
description: Clone a repo with opensrc
---

Clone a repository using opensrc

```bash
npx opensrc <repo>
```

Where `<repo>` is extracted from the user request (supports `owner/repo`, `github:owner/repo`, full URL, etc.)

<user-request>
$ARGUMENTS
</user-request>