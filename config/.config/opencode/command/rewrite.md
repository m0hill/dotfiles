---
description: "Improve a user prompt/text for clarity (without doing the task)"
---

Rewrite the user's text so it is clearer, more concise, and easier to follow.

Use any provided file tree or file contents only as context to make the request more specific (correct paths, name relevant files), but do not change code and do not do the task.

Do not use emojis. Do not add a rigid structure. Keep it short. If something essential is missing, ask 1–3 brief questions using question tool.

<user-request>
$ARGUMENTS
</user-request>
